
"use client";

import { useEffect } from "react";

const ThemeScript = () => {
  useEffect(() => {
    // This component is empty because the script is inlined in the layout.tsx
    // It's just a placeholder to indicate we have theme handling
  }, []);

  return null;
};

export default ThemeScript;
